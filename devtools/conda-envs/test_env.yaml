name: test
channels:
  - conda-forge
before_install:
  - sudo apt-get install gfortran libblas-dev liblapack-dev
dependencies:
    # Base depends
  - python
  - pip
  - numba
  - scipy
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - gfortran
    - qml
    - codecov

