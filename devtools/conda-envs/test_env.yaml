name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - numba
  - scipy
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - qml
    - codecov

