name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - numba
  - scipy
  - numpy
  - qcelemental

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - qml
    - codecov
    - git+https://github.com/zachglick/apnet.git
